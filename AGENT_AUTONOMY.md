# Agent autonomy rules

Claude Code と Codex が agmsg（`monitor`、`turn` hook、手動受信のいずれか）を介して、
ユーザーの新しい入力なしに相互応答するときに適用する。

## 連携の基本形

agent 間連携は次の一方向の段階で進める。

1. **Explore（Claude）**: 抽象的な仮説、複数案、反例候補、反証条件を提示する。
2. **Audit（Codex）**: ユーザー決定、凍結 statement、Lean の型、テスト、台帳、設計文書の順で照合し、
   `accept | challenge | clarify | defer` を返す。
3. **Decide（ユーザー）**: 判定の意味、公開 API、理論的不変条項、研究上の選択が変わる場合だけ決定する。
4. **Implement（Codex）**: 確定した作業パケットの範囲だけを実装する。
5. **Verify（Codex）**: G1〜G4 の実出力で結果を証明する。Claude は必要に応じて仕様との対応をレビューする。

agent のメッセージは権威ではなく提案である。抽象的な着眼点の新規性と、事実・推論・実装案の正しさを
別々に評価する。Claude は未検証部分を事実として渡さず、Codex は局所的な誤りを理由に有用な問題提起まで
破棄しない。

同じ論点で証拠の追加がない言い換えを往復しない。1回の challenge 後も一次資料で決着しない場合は
`defer` とし、意味変更ならユーザーへ、単なる実装 feasibility なら最小の読み取り調査へ進む。

## 報告の周期

agent 間のやり取りが **10往復** に達したら、新しい作業を止めてユーザーへ報告し、続行の判断を仰ぐ。

- 「1往復」は、自分の送信とそれに対する相手の応答の組を1回と数える。
- レビュー、質問、承認、差し戻し、短い ack もすべて数える。
- 番号のないメッセージを受け取った場合も1回として保守的に数える。
- メッセージ本文の先頭に `[AUTO n/10]` を付けて現在の往復数を示す。

報告には次を含める。

- どのバッチを進めていたか
- 完了したこと
- 未完了・未検証のこと
- 変更ファイルと git 状態
- test / build の結果
- 次に推奨する作業
- Claude 提案の `accept / revised / rejected / deferred` 件数
- Claude が新たに発見した重要論点と、誤提案の検証・手戻りコスト

ユーザーが明示的に続行を承認したら、カウンタを0に戻して再開する。

## カウンタの扱い

- カウンタをリセットできるのは**ユーザーの明示的な承認だけ**。agent 同士の合意、subagent、
  monitor 通知、過去の包括的な「どんどん進めて」ではリセットできない。
- `/agmsg` や「受信箱を確認して」だけの指示は、作業続行の承認として扱わない。
- 10往復に達したときの最後のメッセージは `[PAUSE]` として送り、受信側は ack を返さない。

## この規則が上書きしないもの

CLAUDE.md の「停止・確認のタイミング」は、この規則と独立に有効である。
往復数に関係なく、次はユーザーへ確認してから行う。

- git の commit / push
- 研究判断（`category/erie_c_axiom_categorical_memo.md §11` の未確定事項）
- 理論の不変条項に抵触しうる設計変更
- 要件・公開 API・判定の意味の変更
- 破壊的操作・不可逆な移行
